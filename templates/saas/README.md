# {{NAME}} — SaaS (Next.js)

Next.js app starter. Add Supabase + Stripe when auth/billing needed.

## Run
```bash
npm install
npm run dev          # http://localhost:3000
```

## Next
- Wire Supabase (auth + db): `npm i @supabase/supabase-js`
- Wire Stripe (billing): `npm i stripe @stripe/stripe-js`
- Deploy: Vercel
